apiVersion: v1
kind: Service
metadata:
  name: {{ include "kube-secman.fullname" . }}
  labels:
{{ include "kube-secman.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "kube-secman.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
