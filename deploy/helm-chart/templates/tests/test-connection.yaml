apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kube-secman.fullname" . }}-test-connection"
  labels:
{{ include "kube-secman.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "kube-secman.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
