{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "kube-secman.serviceAccountName" . }}
  labels:
{{ include "kube-secman.labels" . | indent 4 }}
{{- end -}}
